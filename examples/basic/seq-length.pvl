// -*- tab-width:4 ; indent-tabs-mode:nil -*-
//:: cases SeqLength
//:: tools silicon
//:: verdict Pass

class Test {
    seq<int> sequence;

    requires Perm(this.sequence, write);
    void test() {
        if(sequence.length == 0) {

        }
    }
}
